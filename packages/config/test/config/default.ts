import { BaseConfig } from '../../src';

const config: BaseConfig = {
  isDev: true,
  port: 3000,
  host: 'localhost',
  clientUrl: 'localhost:4200',
  defaultVersion: '1',
};

export default config;
